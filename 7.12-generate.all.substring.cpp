#include <iostream>
using namespace std;
string substring(string s)
{
    if (s.length()==0)
    {
        return "";
    }
    
    
}
int main()
{
    string s;
    cin>>s;
    cout<<substring(s);
    return 0;
}